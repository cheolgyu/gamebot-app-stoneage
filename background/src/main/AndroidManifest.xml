<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.background">

    <uses-permission tools:node="merge" android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission tools:node="merge" android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application>

        <service
            tools:node="merge"
            android:name="com.example.background.service.TouchService"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter >
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name="com.example.background.service.BackgroundService"
            android:foregroundServiceType="mediaProjection" />

        <activity android:name=".MediaProjectionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>


</manifest>